import styled from "styled-components";

const Card = styled.div`
  box-shadow: 0 6px 16px 0 rgba(0, 25, 49, 0.12);
  border-radius: 10px;
  background: white;
  overflow: hidden;
`;

export default Card;
