import styled from "styled-components";

const Card = styled.div`
  box-shadow: 0 6px 16px 0 #e1e4e7;
  border-radius: 10px;
  background: white;
  overflow: hidden;
  padding: 25px;
`;

export default Card;
